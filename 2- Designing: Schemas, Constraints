##DONUTS SHOP tables

CREATE TABLE IF NOT EXISTS "ingredients"
(
    "id" INTEGER PRIMARY KEY,
    "name" TEXT UNIQUE NOT NULL,
    "usd_per_g" REAL NOT NULL CHECK("usd_per_g" > 0)
);

CREATE TABLE IF NOT EXISTS "donuts"
(
    "id" INTEGER PRIMARY KEY,
    "name" TEXT UNIQUE NOT NULL,
    "gluten" TEXT NOT NULL,
    "price" REAL NOT NULL CHECK("price" > 0)
);

CREATE TABLE IF NOT EXISTS "donuts_ingredients"
(
    "donut_id" INTEGER NOT NULL FOREIGN KEY REFERENCES "donuts"("id"),
    "ingredient_id" INTEGER NOT NULL FOREIGN KEY REFERENCES "ingredients"("id"),
    PRIMARY KEY("donut_id", "ingredient_id")
);

CREATE TABLE IF NOT EXISTS "orders"
(
    "id" INTEGER PRIMARY KEY,
    "customer_id" INTEGER NOT NULL FOREIGN KEY REFERENCES "customers"("id")
);

CREATE TABLE IF NOT EXISTS "orders_donuts"
(
    "order_id" INTEGER NOT NULL FOREIGN KEY REFERENCES "orders"("id"),
    "donut_id" INTEGER NOT NULL FOREIGN KEY REFERENCES "donuts"("id"),
    PRIMARY KEY("order_id", "donut_id")
);

CREATE TABLE IF NOT EXISTS "customers"
(
    "id" INTEGER PRIMARY KEY,
    "first_name" TEXT NOT NULL,
    "last_name" TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS "customers_history"
(
    "customer_id" INTEGER NOT NULL FOREIGN KEY REFERENCES "customers"("id"),
    "order_id" INTEGER NOT NULL FOREIGN KEY REFERENCES "orders"("id"),
    PRIMARY KEY("customer_id", "order_id")
);



##LINKEDIN tables

CREATE TABLE IF NOT EXISTS "users"
(
    "id" INTEGER PRIMARY KEY,
    "first_name" TEXT NOT NULL,
    "last_name" TEXT NOT NULL,
    "username" TEXT UNIQUE NOT NULL,
    "password" TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS "schools"
(
    "id" INTEGER PRIMARY KEY,
    "name" TEXT UNIQUE NOT NULL,
    "type" TEXT NOT NULL,
    "location" TEXT,
    "founded" INTEGER
);

CREATE TABLE IF NOT EXISTS "companies"
(
    "id" INTEGER PRIMARY KEY,
    "name" TEXT UNIQUE NOT NULL,
    "industry" TEXT NOT NULL,
    "location" TEXT
);


CREATE TABLE IF NOT EXISTS "followings"
(
    "followed_id" INTEGER NOT NULL FOREIGN KEY REFERENCES "users"("id"),
    "follower_id" INTEGER NOT NULL FOREIGN KEY REFERENCES "users"("id")
);

CREATE TABLE IF NOT EXISTS "school_affiliations"
(
    "school_id" INTEGER NOT NULL FOREIGN KEY REFERENCES "schools"("id"),
    "user_id" INTEGER NOT NULL FOREIGN KEY REFERENCES "users"("id"),
    "start_date" TEXT NOT NULL,
    "end_date" TEXT,
    "degree" TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS "company_affiliations"
(
    "company_id" INTEGER NOT NULL FOREIGN KEY REFERENCES "companies"("id"),
    "user_id" INTEGER NOT NULL FOREIGN KEY REFERENCES "users"("id"),
    "start_date" NUMERIC NOT NULL,
    "end_date" NUMERIC,
    "title" TEXT NOT NULL
);



##ATLANTA AIRPORT (ATL) tables

CREATE TABLE IF NOT EXISTS "passengers"
(
    "id" INTEGER PRIMARY KEY,
    "first_name" TEXT NOT NULL,
    "last_name" TEXT NOT NULL,
    "age" INTEGER
);

CREATE TABLE IF NOT EXISTS "checkins"
(
    "id" INTEGER PRIMARY KEY,
    "dateandtime" NUMERIC NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "passenger_id" INTEGER FOREIGN KEY REFERENCES "passengers"("id"),
    "flight_id" INTEGER FOREIGN KEY REFERENCES "flights"("id")
);

CREATE TABLE IF NOT EXISTS "airlines"
(
    "id" INTEGER PRIMARY KEY,
    "name" TEXT UNIQUE NOT NULL,
    "concourse(s)" TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS "flights"
(
    "id" INTEGER PRIMARY KEY,
    "number" TEXT NOT NULL,
    "airline_id" INTEGER FOREIGN KEY REFERENCES "airlines"("id"),
    "departs_from_code" TEXT NOT NULL,
    "arrives_to_code" TEXT NOT NULL,
    "departure_time" TEXT NOT NULL,
    "arrival_time" TEXT NOT NULL
);

